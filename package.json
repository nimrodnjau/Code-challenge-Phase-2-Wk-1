{
  "name": "react-hooks-bank-of-flatiron-code-challenge",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "^4.0.1",
    "semantic-ui-css": "^2.2.10"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "json-server": "^0.15.1"
  },
  "scripts": {
    "start": "PORT=8000 react-scripts --openssl-legacy-provider start",
    "build":"react-scripts --openssl-legacy-provider build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "server": "https://code-challenge-phase-2-wk-1-2.onrender.com"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
